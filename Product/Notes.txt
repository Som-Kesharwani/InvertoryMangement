DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "OPTIONS": {
            "service": "my_service",
            "passfile": ".my_pgpass",
        },
    }
}


'default': {
        'ENGINE': 'django.db.backends.postgresql', #'psqlextra.backend',
        'HOST': 'localhost',
        'NAME': 'postgres',#'lusiadas',
        'USER': 'postgres',
        'PASSWORD': 'root',
    }